# Database Configuration (XAMPP MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sif_201_system

# Server Configuration
PORT=3000
SESSION_SECRET=Olivarez_PASS KEY
SESSION_TIMEOUT_MINUTES=30

# Google OAuth Configuration
# Obtain from Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Allowed Google Workspace Domain (Only accounts from this domain can sign in)
ALLOWED_DOMAIN=olivarezcollege.edu.ph

# Admin Security Passcode (Required when granting or delegating administrator permissions)
ADMIN_SECURITY_PASSCODE=OC-Admin2026!
